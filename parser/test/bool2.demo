procedure main {
 int one, zero, temp;
 
 one  = 1;
 zero = 0;

 temp = one or zero;
 if (temp) then
    write one;
 else
    write zero;

 temp = one and zero;
 if (temp) then
    write one;
 else
    write zero;

 temp = not one;
 write temp;

 temp = not zero;
 write temp;
}