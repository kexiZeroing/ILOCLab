procedure main {
  int one, two, three;

  one = 1;
  two = 2;
  three = 3;

  if (one < two)
     then if (one == 0) 
            then { write one ;}
            else { write two ;}
     else write three;
	
}